# Verified against public Finney reads. The bridge uses the 10.x Subtensor API.
# Install in a clean virtualenv: legacy scalecodec conflicts with cyscale.
bittensor==10.5.0
async-substrate-interface==2.2.1
cyscale==0.5.0
