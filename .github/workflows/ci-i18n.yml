name: i18n Audit

on:
  pull_request:
    branches:
      - dev
  push:
    branches:
      - dev

permissions:
  contents: read

jobs:
  i18n-audit:
    # Use GitHub-hosted Ubuntu so this required check does not wait forever
    # when Blacksmith runners are unavailable.
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - name: Run i18n audit
        run: node scripts/i18n-audit.mjs --ci
